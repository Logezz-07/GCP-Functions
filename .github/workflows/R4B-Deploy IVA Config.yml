name: R4B-Deploy IVA Config

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Select environment to deploy"
        required: true
        type: choice
        options:
          - Dev
          - QA
          - Prod
      dry_run:
        description: "Run as Dry Run (Terraform Plan only)?"
        required: true
        type: boolean
      git_tag:
        description: "Git tag or commit to checkout"
        required: true

jobs:
  deploy-config:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.git_tag }}

      # 2️⃣ Validate JSON file
      - name: Validate config.json format
        run: |
          echo "🔍 Checking iva-configs/config.json for valid JSON..."
          cat iva-configs/config.json | jq empty
          echo "✅ JSON validation passed!"

      # 3️⃣ Authenticate to Google Cloud
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # 4️⃣ Setup Terraform
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      # 5️⃣ Terraform Init
      - name: Terraform Init
        working-directory: iva-configs/terraform
        run: |
          ENVIRONMENT=$(echo "${{ github.event.inputs.environment }}" | tr '[:upper:]' '[:lower:]')
          echo "Initializing Terraform backend for environment: $ENVIRONMENT"
          terraform init \
            -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}" \
            -backend-config="prefix=iva-config-${ENVIRONMENT}-state"

      # 6️⃣ Terraform Plan
      - name: Terraform Plan
        working-directory: iva-configs/terraform
        run: |
          echo "Running Terraform Plan for IVA config upload..."
          terraform plan \
            -out=tfplan.out \
            -var="project_id=${{ vars.PROJECT_ID }}" \
            -var="bucket_name=${{ vars.CONFIG_BUCKET_NAME }}"

      # 7️⃣ Terraform Apply (only if dry_run=false)
      - name: Terraform Apply
        if: ${{ github.event.inputs.dry_run == 'false' }}
        working-directory: iva-configs/terraform
        run: |
          echo "Applying Terraform Plan..."
          terraform apply -auto-approve tfplan.out
